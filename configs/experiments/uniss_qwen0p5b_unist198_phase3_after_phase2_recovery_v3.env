# Phase3 handoff for the low-LR Phase2 v3 recovery. Phase3 keeps the v2 global
# shuffle/full-validation fixes and adopts the same conservative LR, warmup,
# and clipping settings. No Phase3 early-stop policy is enabled.

USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
ACTIVATE_SCRIPT="${ACTIVATE_SCRIPT:-${USER_ROOT}/env_recovery/uniss-train-20260721/activate_uniss.sh}"
ENV_ROOT="${ENV_ROOT:-${USER_ROOT}/conda_envs/uniss-train}"

PHASE2_NAME="${PHASE2_NAME:-uniss_qwen0p5b_phase2_unist198_recovery_global_shuffle_lr1e5_v3}"
PHASE2_SAVE_DIR="${PHASE2_SAVE_DIR:-${REPO_ROOT}/checkpoints/${PHASE2_NAME}/full}"
PHASE2_RUN_DIR="${PHASE2_RUN_DIR:-${REPO_ROOT}/runs/${PHASE2_NAME}/full}"
PHASE2_TENSORBOARD_DIR="${PHASE2_TENSORBOARD_DIR:-${PHASE2_RUN_DIR}/tensorboard}"
PHASE2_LOG_PATH="${PHASE2_LOG_PATH:-${REPO_ROOT}/logs/${PHASE2_NAME}_full.log}"
PHASE2_TRAIN_ITERS="${PHASE2_TRAIN_ITERS:-10781}"
PHASE2_SOURCE_ITERATION="${PHASE2_SOURCE_ITERATION:-4600}"
PHASE2_EFFECTIVE_FINAL_ITERATION="${PHASE2_EFFECTIVE_FINAL_ITERATION:-15381}"
PHASE2_PIPELINE_PATTERN="${PHASE2_PIPELINE_PATTERN:-run_qwen0p5b_unist198_phase2_recovery_v3_pipeline.sh}"

PHASE2_MAX_VALID_LOSS="${PHASE2_MAX_VALID_LOSS:-4.5}"
PHASE2_MAX_LAST_VALID_LOSS="${PHASE2_MAX_LAST_VALID_LOSS:-4.3}"
PHASE2_GRAD_SPIKE_THRESHOLD="${PHASE2_GRAD_SPIKE_THRESHOLD:-10.0}"
PHASE2_MAX_CONSECUTIVE_GRAD_SPIKES="${PHASE2_MAX_CONSECUTIVE_GRAD_SPIKES:-1}"
PHASE2_ABSOLUTE_MAX_GRAD_NORM="${PHASE2_ABSOLUTE_MAX_GRAD_NORM:-20.0}"

PHASE3_NAME="${PHASE3_NAME:-uniss_qwen0p5b_phase3_unist198_from_phase2_recovery_v3}"
PHASE3_TRAIN="${PHASE3_TRAIN:-${REPO_ROOT}/data/megatron/phase3_unist198/packed_train.jsonl}"
PHASE3_VALID="${PHASE3_VALID:-${REPO_ROOT}/data/megatron/validation_unist_dev/phase3_valid_packed.jsonl}"
PHASE3_SAVE_DIR="${PHASE3_SAVE_DIR:-${REPO_ROOT}/checkpoints/${PHASE3_NAME}}"
PHASE3_RUN_DIR="${PHASE3_RUN_DIR:-${REPO_ROOT}/runs/${PHASE3_NAME}}"
PHASE3_TENSORBOARD_DIR="${PHASE3_TENSORBOARD_DIR:-${PHASE3_RUN_DIR}/tensorboard}"
TENSORBOARD_ROOT="${TENSORBOARD_ROOT:-${PHASE3_TENSORBOARD_DIR}}"
PHASE3_TENSORBOARD_PORT="${PHASE3_TENSORBOARD_PORT:-6013}"
PHASE3_TENSORBOARD_SESSION="${PHASE3_TENSORBOARD_SESSION:-uniss_phase3_recovery_v3_tensorboard}"
PHASE3_LOG_PATH="${PHASE3_LOG_PATH:-${REPO_ROOT}/logs/${PHASE3_NAME}.log}"
PHASE3_TRAIN_ITERS="${PHASE3_TRAIN_ITERS:-9075}"
PHASE3_LR="${PHASE3_LR:-1e-5}"
PHASE3_MIN_LR="${PHASE3_MIN_LR:-1e-6}"
PHASE3_LR_WARMUP_ITERS="${PHASE3_LR_WARMUP_ITERS:-200}"
PHASE3_MASTER_PORT="${PHASE3_MASTER_PORT:-29629}"

CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-0,1,2,3,4,5,6,7}"
NPROC_PER_NODE="${NPROC_PER_NODE:-8}"
TP="${TP:-1}"
PP="${PP:-1}"
MICRO_BATCH_SIZE="${MICRO_BATCH_SIZE:-2}"
GLOBAL_BATCH_SIZE="${GLOBAL_BATCH_SIZE:-128}"
DATALOADER_TYPE="${DATALOADER_TYPE:-cyclic}"
NO_DATA_SHARDING="${NO_DATA_SHARDING:-1}"
FULL_VALIDATION="${FULL_VALIDATION:-1}"
EVAL_MICRO_BATCH_SIZE="${EVAL_MICRO_BATCH_SIZE:-1}"
EVAL_GLOBAL_BATCH_SIZE="${EVAL_GLOBAL_BATCH_SIZE:-8}"
SEED="${SEED:-20260726}"
CLIP_GRAD="${CLIP_GRAD:-0.5}"

SAVE_INTERVAL="${SAVE_INTERVAL:-100}"
EVAL_INTERVAL="${EVAL_INTERVAL:-100}"
EVAL_ITERS="${EVAL_ITERS:-10}"
LOG_INTERVAL="${LOG_INTERVAL:-10}"
TENSORBOARD_LOG_INTERVAL="${TENSORBOARD_LOG_INTERVAL:-10}"
TENSORBOARD_MEMORY_INTERVAL="${TENSORBOARD_MEMORY_INTERVAL:-10}"
POLL_SECONDS="${POLL_SECONDS:-60}"
