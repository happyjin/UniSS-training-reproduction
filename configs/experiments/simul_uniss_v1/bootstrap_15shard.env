# Isolated 15-shard bootstrap configuration for Simul-UniSS.

USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
ACTIVATE_SCRIPT="${ACTIVATE_SCRIPT:-${USER_ROOT}/env_recovery/uniss-train-20260721/activate_uniss.sh}"

SIMUL_EXPERIMENT="${SIMUL_EXPERIMENT:-simul_uniss_v1_bootstrap_15shard}"
UNIST_ROOT="${UNIST_ROOT:-${REPO_ROOT}/data/raw/UniST}"
TOKENIZER_DIR="${TOKENIZER_DIR:-${REPO_ROOT}/pretrained_models/UniSS}"
SHARD_START="${SHARD_START:-0}"
SHARD_COUNT="${SHARD_COUNT:-15}"

CHUNK_MS="${CHUNK_MS:-640}"
WAIT_K_CHUNKS="${WAIT_K_CHUNKS:-2}"
MAX_PHRASE_TOKENS="${MAX_PHRASE_TOKENS:-16}"
SEQ_LENGTH="${SEQ_LENGTH:-4096}"

PROCESSED_DIR="${PROCESSED_DIR:-${REPO_ROOT}/data/processed/simul_uniss_v1/bootstrap_15shard}"
PACKED_DIR="${PACKED_DIR:-${REPO_ROOT}/data/megatron/simul_uniss_v1/bootstrap_15shard}"
RUN_DIR="${RUN_DIR:-${REPO_ROOT}/runs/simul_uniss_v1/bootstrap_15shard}"
TENSORBOARD_DIR="${TENSORBOARD_DIR:-${RUN_DIR}/tensorboard}"
LOG_DIR="${LOG_DIR:-${REPO_ROOT}/logs/simul_uniss_v1}"

SCHEDULES_JSONL="${SCHEDULES_JSONL:-${PROCESSED_DIR}/schedules.jsonl}"
SAMPLES_JSONL="${SAMPLES_JSONL:-${PROCESSED_DIR}/samples.jsonl}"
PACKED_TRAIN="${PACKED_TRAIN:-${PACKED_DIR}/packed_train.jsonl}"
ACTION_SAMPLES_JSONL="${ACTION_SAMPLES_JSONL:-${PROCESSED_DIR}/action_samples.jsonl}"
ACTION_PACKED_TRAIN="${ACTION_PACKED_TRAIN:-${PACKED_DIR}/packed_action_train.jsonl}"
ACTION_PREPARE_MARKER="${ACTION_PREPARE_MARKER:-${PACKED_DIR}/ACTION_PREPARE_COMPLETE}"
STAGE0_METRICS="${STAGE0_METRICS:-${RUN_DIR}/stage0_metrics.json}"
STAGE0_AUDIO_DIR="${STAGE0_AUDIO_DIR:-${PROCESSED_DIR}/reconstructed_audio}"
STAGE0_PREFIX_DIR="${STAGE0_PREFIX_DIR:-${RUN_DIR}/stage0_prefix}"
STAGE0_PREFIX_TENSORBOARD_DIR="${STAGE0_PREFIX_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage0_prefix}"
STAGE0_DEVICE="${STAGE0_DEVICE:-cuda:0}"
GLM_TOKENIZER_DIR="${GLM_TOKENIZER_DIR:-${REPO_ROOT}/pretrained_models/UniSS/glm4_tokenizer}"

UNIST_VALID_PARQUET="${UNIST_VALID_PARQUET:-${UNIST_ROOT}/dev-00000.parquet}"
VALID_PROCESSED_DIR="${VALID_PROCESSED_DIR:-${REPO_ROOT}/data/processed/simul_uniss_v1/validation_dev}"
VALID_PACKED_DIR="${VALID_PACKED_DIR:-${REPO_ROOT}/data/megatron/simul_uniss_v1/validation_dev}"
VALID_SCHEDULES_JSONL="${VALID_SCHEDULES_JSONL:-${VALID_PROCESSED_DIR}/schedules.jsonl}"
VALID_SAMPLES_JSONL="${VALID_SAMPLES_JSONL:-${VALID_PROCESSED_DIR}/samples.jsonl}"
VALID_ACTION_SAMPLES_JSONL="${VALID_ACTION_SAMPLES_JSONL:-${VALID_PROCESSED_DIR}/action_samples.jsonl}"
VALID_PACKED_INTERLEAVED="${VALID_PACKED_INTERLEAVED:-${VALID_PACKED_DIR}/packed_interleaved_valid.jsonl}"
VALID_PACKED_ACTION="${VALID_PACKED_ACTION:-${VALID_PACKED_DIR}/packed_action_valid.jsonl}"

# Read-only initialization root. Megatron reads latest_checkpointed_iteration.txt.
QWEN_CHECKPOINT_ROOT="${QWEN_CHECKPOINT_ROOT:-${REPO_ROOT}/checkpoints/uniss_qwen0p5b_phase1_unist198_recovery_b1_v2}"
SIMUL_CHECKPOINT_ROOT="${SIMUL_CHECKPOINT_ROOT:-${REPO_ROOT}/checkpoints/simul_uniss_v1}"

TENSORBOARD_PORT="${TENSORBOARD_PORT:-6008}"
TENSORBOARD_SESSION="${TENSORBOARD_SESSION:-simul_uniss_v1_tensorboard}"

POLICY_TOKENIZER_DIR="${POLICY_TOKENIZER_DIR:-${PROCESSED_DIR}/policy_tokenizer}"
POLICY_TOKENIZER_MODEL="${POLICY_TOKENIZER_MODEL:-${POLICY_TOKENIZER_DIR}/policy_8k.model}"
POLICY_VOCAB_SIZE="${POLICY_VOCAB_SIZE:-8192}"

STAGE1_OUTPUT_DIR="${STAGE1_OUTPUT_DIR:-${SIMUL_CHECKPOINT_ROOT}/stage1_bootstrap_student_15shard}"
STAGE1_DEVICE="${STAGE1_DEVICE:-cuda}"
STAGE1_BATCH_SIZE="${STAGE1_BATCH_SIZE:-2}"
STAGE1_MAX_STEPS="${STAGE1_MAX_STEPS:-1000}"
STAGE1_LEARNING_RATE="${STAGE1_LEARNING_RATE:-1e-4}"
STAGE1_HIDDEN_SIZE="${STAGE1_HIDDEN_SIZE:-256}"
STAGE1_NUM_LAYERS="${STAGE1_NUM_LAYERS:-4}"
STAGE1_NUM_HEADS="${STAGE1_NUM_HEADS:-8}"
STAGE1_MAX_SOURCE_TOKENS="${STAGE1_MAX_SOURCE_TOKENS:-1024}"
STAGE1_VALIDATION_RECORDS="${STAGE1_VALIDATION_RECORDS:-128}"
STAGE1_EVAL_INTERVAL="${STAGE1_EVAL_INTERVAL:-100}"
STAGE1_SAVE_INTERVAL="${STAGE1_SAVE_INTERVAL:-100}"
STAGE1_AUDIO_OUTPUT_DIR="${STAGE1_AUDIO_OUTPUT_DIR:-${SIMUL_CHECKPOINT_ROOT}/stage1_audio_student_15shard}"
STAGE1_AUDIO_TENSORBOARD_DIR="${STAGE1_AUDIO_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage1_audio}"
STAGE1_AUDIO_DEVICE="${STAGE1_AUDIO_DEVICE:-cuda:0}"
STAGE1_AUDIO_BATCH_SIZE="${STAGE1_AUDIO_BATCH_SIZE:-2}"
STAGE1_AUDIO_MAX_STEPS="${STAGE1_AUDIO_MAX_STEPS:-1000}"

SIMUL_CUDA_VISIBLE_DEVICES="${SIMUL_CUDA_VISIBLE_DEVICES:-0,1,2,3,4,5,6,7}"
SIMUL_NPROC_PER_NODE="${SIMUL_NPROC_PER_NODE:-8}"
SIMUL_MICRO_BATCH_SIZE="${SIMUL_MICRO_BATCH_SIZE:-1}"
SIMUL_GLOBAL_BATCH_SIZE="${SIMUL_GLOBAL_BATCH_SIZE:-32}"
SIMUL_QWEN_LR="${SIMUL_QWEN_LR:-1e-5}"
SIMUL_QWEN_MIN_LR="${SIMUL_QWEN_MIN_LR:-1e-6}"
SIMUL_QWEN_WARMUP_ITERS="${SIMUL_QWEN_WARMUP_ITERS:-10}"
SIMUL_QWEN_SAVE_INTERVAL="${SIMUL_QWEN_SAVE_INTERVAL:-50}"
SIMUL_QWEN_EVAL_INTERVAL="${SIMUL_QWEN_EVAL_INTERVAL:-20}"
SIMUL_QWEN_EVAL_ITERS="${SIMUL_QWEN_EVAL_ITERS:-5}"

STAGE3_LOAD_ROOT="${STAGE3_LOAD_ROOT:-${QWEN_CHECKPOINT_ROOT}}"
STAGE3_SAVE_ROOT="${STAGE3_SAVE_ROOT:-${SIMUL_CHECKPOINT_ROOT}/stage3_action_qwen_15shard}"
STAGE3_TENSORBOARD_DIR="${STAGE3_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage3_action}"
STAGE3_MASTER_PORT="${STAGE3_MASTER_PORT:-29631}"
STAGE3_TRAIN_ITERS="${STAGE3_TRAIN_ITERS:-200}"

STAGE4_LOAD_ROOT="${STAGE4_LOAD_ROOT:-${STAGE3_SAVE_ROOT}}"
STAGE4_SAVE_ROOT="${STAGE4_SAVE_ROOT:-${SIMUL_CHECKPOINT_ROOT}/stage4_interleaved_qwen_15shard}"
STAGE4_TENSORBOARD_DIR="${STAGE4_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage4_interleaved}"
STAGE4_MASTER_PORT="${STAGE4_MASTER_PORT:-29632}"
STAGE4_TRAIN_ITERS="${STAGE4_TRAIN_ITERS:-500}"

STAGE6_LOAD_ROOT="${STAGE6_LOAD_ROOT:-${STAGE4_SAVE_ROOT}}"
STAGE6_SAVE_ROOT="${STAGE6_SAVE_ROOT:-${SIMUL_CHECKPOINT_ROOT}/stage6_joint_low_lr_15shard}"
STAGE6_TENSORBOARD_DIR="${STAGE6_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage6_joint}"
STAGE6_MASTER_PORT="${STAGE6_MASTER_PORT:-29633}"
STAGE6_TRAIN_ITERS="${STAGE6_TRAIN_ITERS:-200}"
STAGE6_QWEN_LR="${STAGE6_QWEN_LR:-3e-6}"
STAGE6_QWEN_MIN_LR="${STAGE6_QWEN_MIN_LR:-5e-7}"

STAGE5_OUTPUT_DIR="${STAGE5_OUTPUT_DIR:-${RUN_DIR}/stage5_streaming_replay}"
STAGE5_TENSORBOARD_DIR="${STAGE5_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage5_streaming_replay}"
BICODEC_MODEL_DIR="${BICODEC_MODEL_DIR:-${REPO_ROOT}/pretrained_models/UniSS/bicodec}"
BICODEC_DEVICE="${BICODEC_DEVICE:-cuda:0}"
STAGE5_REFINEMENT_OUTPUT_DIR="${STAGE5_REFINEMENT_OUTPUT_DIR:-${SIMUL_CHECKPOINT_ROOT}/stage5_bicodec_refinement_15shard}"
STAGE5_REFINEMENT_TENSORBOARD_DIR="${STAGE5_REFINEMENT_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage5_bicodec_refinement}"
STAGE5_REFINEMENT_BATCH_SIZE="${STAGE5_REFINEMENT_BATCH_SIZE:-1}"
STAGE5_REFINEMENT_MAX_STEPS="${STAGE5_REFINEMENT_MAX_STEPS:-500}"

STAGE7_OUTPUT_DIR="${STAGE7_OUTPUT_DIR:-${SIMUL_CHECKPOINT_ROOT}/stage7_policy_grpo_15shard}"
STAGE7_TENSORBOARD_DIR="${STAGE7_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage7_grpo}"
STAGE7_DEVICE="${STAGE7_DEVICE:-cuda}"
STAGE7_BATCH_SIZE="${STAGE7_BATCH_SIZE:-128}"
STAGE7_SFT_STEPS="${STAGE7_SFT_STEPS:-200}"
STAGE7_GRPO_STEPS="${STAGE7_GRPO_STEPS:-500}"
STAGE7_GROUP_SIZE="${STAGE7_GROUP_SIZE:-8}"

STAGE8_OUTPUT_DIR="${STAGE8_OUTPUT_DIR:-${SIMUL_CHECKPOINT_ROOT}/stage8_nar_semantic_15shard}"
STAGE8_TENSORBOARD_DIR="${STAGE8_TENSORBOARD_DIR:-${TENSORBOARD_DIR}/stage8_nar}"
STAGE8_DEVICE="${STAGE8_DEVICE:-cuda}"
STAGE8_BATCH_SIZE="${STAGE8_BATCH_SIZE:-8}"
STAGE8_MAX_STEPS="${STAGE8_MAX_STEPS:-1000}"
