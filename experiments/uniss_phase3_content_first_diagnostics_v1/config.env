#!/usr/bin/env bash

USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
REPO_ROOT="${REPO_ROOT:-${USER_ROOT}/projects/UniSS}"
ENV_ROOT="${ENV_ROOT:-${USER_ROOT}/conda_envs/uniss-train}"
PYTHON="${PYTHON:-${ENV_ROOT}/bin/python}"

EXPERIMENT_NAME="${EXPERIMENT_NAME:-uniss_phase3_content_first_diagnostics_v1}"
REPORT_ROOT="${REPORT_ROOT:-${REPO_ROOT}/reports/uniss_phase3_content_first_joint_s2st_v1/root_cause_and_next_plan_v1}"
LOG_ROOT="${LOG_ROOT:-${REPO_ROOT}/logs/${EXPERIMENT_NAME}}"

EPISODES="${EPISODES:-${REPO_ROOT}/data/processed/uniss_phasea_event_constrained_grpo_long_v2/protocol64_v1/episodes.jsonl}"
WHISPERVQ_MODEL="${WHISPERVQ_MODEL:-${REPO_ROOT}/pretrained_models/UniSS/glm4_tokenizer}"
BICODEC_MODEL="${BICODEC_MODEL:-${REPO_ROOT}/pretrained_models/UniSS/bicodec}"
BASE_HF="${BASE_HF:-${REPO_ROOT}/checkpoints/exported_hf/qwen0p5b_phase3_unist198_iter_0009075_hf}"
SOURCE_SNAPSHOT="${SOURCE_SNAPSHOT:-${REPO_ROOT}/data/processed/uniss_phase3_v4_quality_first_true_streaming_pilot15_v1/stage_a_causal_asr/source_snapshot_v5.json}"
BASELINE_ROLLOUT="${BASELINE_ROLLOUT:-${REPO_ROOT}/eval_outputs/uniss_phasea_stateful_longepisode_rl_v1/formal_train64_g4_v1/ROLLOUT_MERGED.json}"

CONTENT_FIRST_SFT="${CONTENT_FIRST_SFT:-${REPO_ROOT}/checkpoints/uniss_phase3_content_first_joint_s2st_v1_formal1e_v1/iter_0000717}"
CONTENT_FIRST_RUNTIME_EXPORT="${CONTENT_FIRST_RUNTIME_EXPORT:-${REPO_ROOT}/reports/uniss_phase3_content_first_joint_s2st_v1_formal1e_v1/runtime_exports/iter_0000717}"

GPU_HOLDER_SESSION="${GPU_HOLDER_SESSION:-uniss_gpu_load_60}"
