#!/usr/bin/env bash

USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
REPO_ROOT="${REPO_ROOT:-${USER_ROOT}/projects/UniSS}"
ENV_ROOT="${ENV_ROOT:-${USER_ROOT}/conda_envs/uniss-train}"
PYTHON="${PYTHON:-${ENV_ROOT}/bin/python}"

EXPERIMENT_NAME="${EXPERIMENT_NAME:-uniss_phase3_event_rollout_joint_pilot15_v1}"
HERE="${REPO_ROOT}/experiments/uniss_phase3_event_rollout_joint_pilot15_v1"
PHASE3_NATIVE_CHECKPOINT="${REPO_ROOT}/checkpoints/uniss_qwen0p5b_phase3_unist198_after_phase2_v4"
PHASE3_REPLAY_PACKED="${REPO_ROOT}/data/megatron/uniss_true_subsecond_pilot15_v1/packed_replay.jsonl"
PHASE3_REPLAY_OFFSETS="${REPO_ROOT}/data/megatron/uniss_true_subsecond_pilot15_v1/packed_replay.offsets.u64"
TRAJECTORY_MANIFEST="${TRAJECTORY_MANIFEST:-${REPO_ROOT}/data/megatron/uniss_phase3_event_rollout_joint_pilot15_v1/train_parts_manifest.json}"
VALID_TRAJECTORY_MANIFEST="${VALID_TRAJECTORY_MANIFEST:-${REPO_ROOT}/data/megatron/uniss_phase3_event_rollout_joint_pilot15_v1/valid_parts_manifest.json}"
DATA_AUDIT="${DATA_AUDIT:-${REPO_ROOT}/reports/uniss_phase3_event_rollout_joint_pilot15_v1/data_audit_full.json}"
TRAINING_ROOT="${TRAINING_ROOT:-${REPO_ROOT}/data/megatron/${EXPERIMENT_NAME}/training}"
TRAINING_MANIFEST="${TRAINING_MANIFEST:-${TRAINING_ROOT}/training_manifest.json}"
REPLAY_SUBSET_OFFSETS="${REPLAY_SUBSET_OFFSETS:-${TRAINING_ROOT}/replay_subset.u64}"
SAVE_DIR="${SAVE_DIR:-${REPO_ROOT}/checkpoints/${EXPERIMENT_NAME}}"
RUN_DIR="${RUN_DIR:-${REPO_ROOT}/runs/${EXPERIMENT_NAME}}"
TB_DIR="${TB_DIR:-${RUN_DIR}/tensorboard}"
LOG_PATH="${LOG_PATH:-${REPO_ROOT}/logs/${EXPERIMENT_NAME}.log}"
REPORT_ROOT="${REPORT_ROOT:-${REPO_ROOT}/reports/${EXPERIMENT_NAME}}"

CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-0,1,2,3,4,5,6,7}"
MASTER_PORT="${MASTER_PORT:-29921}"
TENSORBOARD_PORT="${TENSORBOARD_PORT:-6092}"
MICRO_BATCH_SIZE="${MICRO_BATCH_SIZE:-2}"
GLOBAL_BATCH_SIZE="${GLOBAL_BATCH_SIZE:-128}"
SEQ_LENGTH="${SEQ_LENGTH:-18000}"
COVERAGE_EPOCHS="${COVERAGE_EPOCHS:-1}"
REPLAY_FRACTION="${REPLAY_FRACTION:-0.35}"
SEED="${SEED:-20260812}"
LR_QWEN_LORA="${LR_QWEN_LORA:-1e-5}"
LR_FRONTEND="${LR_FRONTEND:-5e-6}"
LR_NEW_HEADS="${LR_NEW_HEADS:-5e-5}"
MIN_LR="${MIN_LR:-1e-6}"
WEIGHT_DECAY="${WEIGHT_DECAY:-0.05}"
SAVE_INTERVAL="${SAVE_INTERVAL:-50}"
EVAL_INTERVAL="${EVAL_INTERVAL:-50}"
EVAL_ITERS="${EVAL_ITERS:-10}"
LOG_INTERVAL="${LOG_INTERVAL:-1}"
NUM_WORKERS="${NUM_WORKERS:-8}"
NPZ_LRU_CAPACITY="${NPZ_LRU_CAPACITY:-8}"
WHISPERVQ_CODEBOOK="${WHISPERVQ_CODEBOOK:-${REPO_ROOT}/pretrained_models/UniSS/glm4_tokenizer/model.safetensors}"
PHASE3_FINGERPRINT="${PHASE3_FINGERPRINT:-${REPO_ROOT}/data/processed/uniss_phase3_true_subsecond_deadline_full198_v1/model_handoff/phase3_embedding_fingerprint.json}"

