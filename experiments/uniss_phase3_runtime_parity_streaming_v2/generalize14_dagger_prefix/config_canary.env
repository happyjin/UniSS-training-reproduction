#!/usr/bin/env bash

USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
REPO_ROOT="${REPO_ROOT:-${USER_ROOT}/projects/UniSS}"
ENV_ROOT="${ENV_ROOT:-${USER_ROOT}/conda_envs/uniss-train}"
PYTHON="${PYTHON:-${ENV_ROOT}/bin/python}"

EXPERIMENT_NAME="uniss_phase3_runtime_parity_streaming_v2_generalize14_dagger_prefix_canary_v1"
V13_CHECKPOINT_ROOT="${REPO_ROOT}/checkpoints/uniss_phase3_runtime_parity_streaming_v2_generalize13_joint_runtime_canary_v2"
V13_BASE_ITERATION=50
PHASE3_NATIVE_CHECKPOINT="${REPO_ROOT}/checkpoints/${EXPERIMENT_NAME}_v13_iter50_base"
PHASE3_REPLAY_PACKED="${REPO_ROOT}/data/megatron/uniss_true_subsecond_pilot15_v1/packed_replay.jsonl"
PHASE3_REPLAY_OFFSETS="${REPO_ROOT}/data/megatron/uniss_true_subsecond_pilot15_v1/packed_replay.offsets.u64"
TRAJECTORY_PACKED="${REPO_ROOT}/data/megatron/uniss_phase3_runtime_parity_streaming_v2/canary128/train.packed.jsonl"
VALID_TRAJECTORY_PACKED="${REPO_ROOT}/data/megatron/uniss_phase3_runtime_parity_streaming_v2/canary128/valid.packed.jsonl"
TRAINING_ROOT="${REPO_ROOT}/data/megatron/${EXPERIMENT_NAME}/training"
TRAINING_MANIFEST="${TRAINING_ROOT}/training_manifest.json"
REPLAY_SUBSET_OFFSETS="${TRAINING_ROOT}/replay_subset.u64"
SAVE_DIR="${REPO_ROOT}/checkpoints/${EXPERIMENT_NAME}"
RUN_DIR="${REPO_ROOT}/runs/${EXPERIMENT_NAME}"
TB_DIR="${RUN_DIR}/tensorboard"
LOG_PATH="${REPO_ROOT}/logs/${EXPERIMENT_NAME}.log"
TENSORBOARD_PORT=6087

CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-0,1,2,3,4,5,6,7}"
MASTER_PORT=29825
MICRO_BATCH_SIZE=2
GLOBAL_BATCH_SIZE=128
SEQ_LENGTH=18000
COVERAGE_EPOCHS=200
REPLAY_FRACTION=0.10
LR_QWEN_LORA=5e-6
LR_FRONTEND=1e-6
LR_NEW_HEADS=2e-5
MIN_LR=1e-6
WEIGHT_DECAY=0.05
ACTION_WRITE_WEIGHT=2.0
SAVE_INTERVAL=25
EVAL_INTERVAL=25
EVAL_ITERS=1
LOG_INTERVAL=1
NUM_WORKERS=8
NPZ_LRU_CAPACITY=8
SEED=20260822
