# Isolated simultaneous/streaming Stage3 action evaluation.

USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
REPO_ROOT="${REPO_ROOT:-${USER_ROOT}/projects/UniSS}"
ENV_ROOT="${ENV_ROOT:-${USER_ROOT}/conda_envs/uniss-train}"
EXPERIMENT_NAME="${EXPERIMENT_NAME:-simul_uniss_stage3_action_v1}"

STAGE3_CHECKPOINT_ROOT="${STAGE3_CHECKPOINT_ROOT:-${REPO_ROOT}/checkpoints/simul_uniss_v7_full198_seq18000_mbs2_gbs128_stage3/stage03_action_sft}"
STAGE3_ITERATION="${STAGE3_ITERATION:-4753}"
HF_REFERENCE="${HF_REFERENCE:-${REPO_ROOT}/checkpoints/qwen2_0p5b_uniss_vocab_hf}"
HF_EXPORT="${HF_EXPORT:-${REPO_ROOT}/checkpoints/exported_hf/${EXPERIMENT_NAME}_iter_0004753_hf}"

TOKENIZER="${TOKENIZER:-${REPO_ROOT}/pretrained_models/UniSS}"
DEV_SAMPLES="${DEV_SAMPLES:-${REPO_ROOT}/data/processed/simul_uniss_v1/validation_dev/action_samples.jsonl}"
DEV_SCHEDULES="${DEV_SCHEDULES:-${REPO_ROOT}/data/processed/simul_uniss_v1/validation_dev/schedules.jsonl}"
EVAL_SOURCE_PARQUET="${EVAL_SOURCE_PARQUET:-${REPO_ROOT}/data/raw/UniST/test-00000.parquet}"
EVAL_DATA_DIR="${EVAL_DATA_DIR:-${REPO_ROOT}/data/evaluation/${EXPERIMENT_NAME}/unist_test_chunk640_wait2}"
EVAL_SAMPLES="${EVAL_SAMPLES:-${EVAL_DATA_DIR}/action_samples.jsonl}"
EVAL_SCHEDULES="${EVAL_SCHEDULES:-${EVAL_DATA_DIR}/schedules.jsonl}"

CHUNK_MS="${CHUNK_MS:-640}"
WAIT_K_CHUNKS="${WAIT_K_CHUNKS:-2}"
MAX_PHRASE_TOKENS="${MAX_PHRASE_TOKENS:-16}"

DTYPE="${DTYPE:-bf16}"
ATTENTION_IMPLEMENTATION="${ATTENTION_IMPLEMENTATION:-flash_attention_2}"
MAX_BATCH_TOKENS="${MAX_BATCH_TOKENS:-262144}"
MAX_BATCH_SIZE="${MAX_BATCH_SIZE:-512}"
LOGIT_EVENT_BATCH="${LOGIT_EVENT_BATCH:-256}"
WARMUP_BATCHES="${WARMUP_BATCHES:-2}"
WARMUP_BATCH_SIZE="${WARMUP_BATCH_SIZE:-16}"

DEV_GPUS="${DEV_GPUS:-0,1,2,3}"
EVAL_GPUS="${EVAL_GPUS:-4,5,6,7}"
DEV_MASTER_PORT="${DEV_MASTER_PORT:-30431}"
EVAL_MASTER_PORT="${EVAL_MASTER_PORT:-30432}"

EXPECTED_DEV_SAMPLES="${EXPECTED_DEV_SAMPLES:-7965}"
EXPECTED_EVAL_SAMPLES="${EXPECTED_EVAL_SAMPLES:-23369}"

OUTPUT_ROOT="${OUTPUT_ROOT:-${REPO_ROOT}/eval_outputs/${EXPERIMENT_NAME}}"
