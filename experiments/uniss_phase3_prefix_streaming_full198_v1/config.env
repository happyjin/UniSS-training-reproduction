#!/usr/bin/env bash

USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
REPO_ROOT="${REPO_ROOT:-${USER_ROOT}/projects/UniSS}"
ENV_ROOT="${ENV_ROOT:-${USER_ROOT}/conda_envs/uniss-train}"
PYTHON="${PYTHON:-${ENV_ROOT}/bin/python}"

RUN_NAME="${RUN_NAME:-uniss_phase3_prefix_streaming_full198_joint_v1}"
PHASE3_MODEL="${PHASE3_MODEL:-${REPO_ROOT}/checkpoints/exported_hf/qwen0p5b_phase3_unist198_iter_0009075_hf}"
INDEX_JSON="${INDEX_JSON:-${REPO_ROOT}/data/processed/uniss_phase3_prefix_streaming_full198_v1/direction_index_valid_v2/index.json}"
VALID_PARQUET="${VALID_PARQUET:-${REPO_ROOT}/data/raw/UniST/dev-00000.parquet}"

SAVE_DIR="${SAVE_DIR:-${REPO_ROOT}/checkpoints/${RUN_NAME}}"
TB_DIR="${TB_DIR:-${REPO_ROOT}/runs/${RUN_NAME}/tensorboard}"
LOG="${LOG:-${REPO_ROOT}/logs/${RUN_NAME}.log}"
GPU_LOG="${GPU_LOG:-${REPO_ROOT}/logs/${RUN_NAME}_gpu_power_utility.csv}"

CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-0,1,2,3,4,5,6,7}"
NPROC_PER_NODE="${NPROC_PER_NODE:-8}"
MASTER_PORT="${MASTER_PORT:-29665}"
TENSORBOARD_PORT="${TENSORBOARD_PORT:-6065}"

TRAIN_ITERS="${TRAIN_ITERS:-12000}"
MICRO_BATCH_SIZE="${MICRO_BATCH_SIZE:-16}"
GLOBAL_BATCH_SIZE="${GLOBAL_BATCH_SIZE:-128}"
LR="${LR:-2e-5}"
MIN_LR="${MIN_LR:-1e-6}"
WARMUP_ITERS="${WARMUP_ITERS:-300}"
NUM_WORKERS="${NUM_WORKERS:-4}"
SAVE_INTERVAL="${SAVE_INTERVAL:-500}"
EVAL_INTERVAL="${EVAL_INTERVAL:-250}"
EVAL_ITERS="${EVAL_ITERS:-8}"
LOG_INTERVAL="${LOG_INTERVAL:-10}"
VALID_LIMIT="${VALID_LIMIT:-1024}"
BLOCK_SIZE="${BLOCK_SIZE:-64}"
SEED="${SEED:-20260809}"
