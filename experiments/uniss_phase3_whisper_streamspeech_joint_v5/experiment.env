REPO_ROOT="${REPO_ROOT:-/opt/dlami/nvme/jasonleeeli/projects/UniSS}"
USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
PYTHON="${PYTHON:-${USER_ROOT}/conda_envs/uniss-train/bin/python}"

WHISPER_MODEL="${WHISPER_MODEL:-${REPO_ROOT}/pretrained_models/UniSS/glm4_tokenizer}"
PHASE3_MODEL="${PHASE3_MODEL:-${REPO_ROOT}/checkpoints/exported_hf/qwen0p5b_phase3_unist198_iter_0009075_hf}"
PHASE3_REPLAY_PACKED="${PHASE3_REPLAY_PACKED:-${REPO_ROOT}/data/megatron/phase3_unist198/packed_train.jsonl}"

V1_DATA_ROOT="${V1_DATA_ROOT:-${REPO_ROOT}/data/processed/phase3_whisper_streamspeech_joint_v1}"
V5_DATA_ROOT="${V5_DATA_ROOT:-${REPO_ROOT}/data/processed/phase3_whisper_streamspeech_joint_v5}"
SMOKE_ROOT="${SMOKE_ROOT:-${V1_DATA_ROOT}/smoke_manifest_128}"
SMOKE_REPLAY_OFFSETS="${SMOKE_REPLAY_OFFSETS:-${V1_DATA_ROOT}/phase3_replay_index/smoke_first8.u64}"
FULL_STAGE_A_ROOT="${FULL_STAGE_A_ROOT:-${V1_DATA_ROOT}/full198_stage_a}"
FULL_REPLAY_OFFSETS="${FULL_REPLAY_OFFSETS:-${V1_DATA_ROOT}/phase3_replay_index/full198.u64}"
UNIST_DEV_STAGE_A="${UNIST_DEV_STAGE_A:-${REPO_ROOT}/data/processed/simul_uniss_subsecond_e2_v1/unist_dev/stage_a_source/manifest.jsonl}"
PILOT_ROOT="${PILOT_ROOT:-${V5_DATA_ROOT}/pilot_15shard_joint}"
PILOT_REPLAY_OFFSETS="${PILOT_REPLAY_OFFSETS:-${V5_DATA_ROOT}/phase3_replay_index/pilot_first100k.u64}"

CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-0,1,2,3,4,5,6,7}"
NPROC_PER_NODE="${NPROC_PER_NODE:-8}"
MASTER_PORT="${MASTER_PORT:-29745}"
MICRO_BATCH_SIZE="${MICRO_BATCH_SIZE:-1}"
GLOBAL_BATCH_SIZE="${GLOBAL_BATCH_SIZE:-128}"
NUM_WORKERS="${NUM_WORKERS:-8}"
SEED="${SEED:-20260806}"

TENSORBOARD_PORT="${TENSORBOARD_PORT:-6032}"
TENSORBOARD_SESSION="${TENSORBOARD_SESSION:-uniss_phase3_joint_v5_tensorboard}"
