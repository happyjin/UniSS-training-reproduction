# Stage 8 — optional NAR semantic branch

This full-data eight-GPU branch is profiling-gated and never starts
automatically. It is used only if the Stage 4/6 autoregressive path misses the
target real-time factor.
