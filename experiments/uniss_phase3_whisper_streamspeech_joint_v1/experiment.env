REPO_ROOT="${REPO_ROOT:-/opt/dlami/nvme/jasonleeeli/projects/UniSS}"
USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
PYTHON="${PYTHON:-${USER_ROOT}/conda_envs/uniss-train/bin/python}"

WHISPER_MODEL="${WHISPER_MODEL:-${REPO_ROOT}/pretrained_models/UniSS/glm4_tokenizer}"
PHASE3_MODEL="${PHASE3_MODEL:-${REPO_ROOT}/checkpoints/exported_hf/qwen0p5b_phase3_unist198_iter_0009075_hf}"
BICODEC_CHECKPOINT="${BICODEC_CHECKPOINT:-${REPO_ROOT}/pretrained_models/UniSS/bicodec/BiCodec}"
PHASE3_REPLAY_PACKED="${PHASE3_REPLAY_PACKED:-${REPO_ROOT}/data/megatron/phase3_unist198/packed_train.jsonl}"

DATA_ROOT="${DATA_ROOT:-${REPO_ROOT}/data/processed/phase3_whisper_streamspeech_joint_v1}"
SMOKE_ROOT="${SMOKE_ROOT:-${DATA_ROOT}/smoke_manifest_128}"
FORMAL_STAGE_A_ROOT="${FORMAL_STAGE_A_ROOT:-${DATA_ROOT}/full198_stage_a}"
FORMAL_JOINT_ROOT="${FORMAL_JOINT_ROOT:-${DATA_ROOT}/full198_joint}"
REPLAY_INDEX_ROOT="${REPLAY_INDEX_ROOT:-${DATA_ROOT}/phase3_replay_index}"
SMOKE_REPLAY_OFFSETS="${SMOKE_REPLAY_OFFSETS:-${REPLAY_INDEX_ROOT}/smoke_first8.u64}"
FULL_REPLAY_OFFSETS="${FULL_REPLAY_OFFSETS:-${REPLAY_INDEX_ROOT}/full198.u64}"

PILOT_STAGE_A_ROOT="${PILOT_STAGE_A_ROOT:-${REPO_ROOT}/data/processed/simul_uniss_subsecond_v1/pilot_15shard/stage_a_source}"
UNIST_DEV_STAGE_A="${UNIST_DEV_STAGE_A:-${REPO_ROOT}/data/processed/simul_uniss_subsecond_e2_v1/unist_dev/stage_a_source/manifest.jsonl}"
UNIST_RAW_ROOT="${UNIST_RAW_ROOT:-${REPO_ROOT}/data/raw/UniST}"

CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-0,1,2,3,4,5,6,7}"
NPROC_PER_NODE="${NPROC_PER_NODE:-8}"
MASTER_PORT="${MASTER_PORT:-29731}"
MICRO_BATCH_SIZE="${MICRO_BATCH_SIZE:-1}"
GLOBAL_BATCH_SIZE="${GLOBAL_BATCH_SIZE:-128}"
NUM_WORKERS="${NUM_WORKERS:-4}"
SEED="${SEED:-20260806}"

TENSORBOARD_PORT="${TENSORBOARD_PORT:-6031}"
TENSORBOARD_SESSION="${TENSORBOARD_SESSION:-uniss_phase3_whisper_streamspeech_joint_tb}"
