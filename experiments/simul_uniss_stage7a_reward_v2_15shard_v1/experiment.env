# Isolated Stage7A Reward-v2 experiments on the original 15-shard bootstrap.

USER_ROOT="${USER_ROOT:-/opt/dlami/nvme/jasonleeeli}"
REPO_ROOT="${REPO_ROOT:-${USER_ROOT}/projects/UniSS}"
TRAIN_ENV="${TRAIN_ENV:-${USER_ROOT}/conda_envs/uniss-train}"
EVAL_ENV="${EVAL_ENV:-${USER_ROOT}/conda_envs/uniss-eval}"

EXPERIMENT_NAME="${EXPERIMENT_NAME:-simul_uniss_stage7a_reward_v2_15shard_v1}"
STAGE6_MODEL="${STAGE6_MODEL:-${REPO_ROOT}/checkpoints/exported_hf/simul_uniss_stage6_streaming_v1_iter_0001189_hf}"
E3_V1_CHECKPOINT="${E3_V1_CHECKPOINT:-${REPO_ROOT}/checkpoints/simul_uniss_stage7a_15shard_v1/e3_grpo_g8_full/best.pt}"
E3_V1_MODEL="${E3_V1_MODEL:-${REPO_ROOT}/checkpoints/exported_hf/simul_uniss_stage7a_15shard_v1/e3_grpo_g8_best_hf}"
SPEECH_TOKENIZER="${SPEECH_TOKENIZER:-${REPO_ROOT}/pretrained_models/UniSS}"

TRAIN_SAMPLES="${TRAIN_SAMPLES:-${REPO_ROOT}/data/processed/simul_uniss_v1/bootstrap_15shard/action_samples.jsonl}"
DEV_SAMPLES="${DEV_SAMPLES:-${REPO_ROOT}/data/processed/simul_uniss_v1/validation_dev/action_samples.jsonl}"
DEV_SCHEDULES="${DEV_SCHEDULES:-${REPO_ROOT}/data/processed/simul_uniss_v1/validation_dev/schedules.jsonl}"
EXPECTED_DEV_RECORDS="${EXPECTED_DEV_RECORDS:-7965}"
OFFLINE_PHASE3_DEV="${OFFLINE_PHASE3_DEV:-${REPO_ROOT}/eval_outputs/qwen0p5b_phase3_unist198_iter_0009075_unist_dev_full_20260726T065924Z}"

CHECKPOINT_ROOT="${CHECKPOINT_ROOT:-${REPO_ROOT}/checkpoints/${EXPERIMENT_NAME}}"
RUN_ROOT="${RUN_ROOT:-${REPO_ROOT}/runs/${EXPERIMENT_NAME}}"
LOG_ROOT="${LOG_ROOT:-${REPO_ROOT}/logs/${EXPERIMENT_NAME}}"
EVAL_ROOT="${EVAL_ROOT:-${REPO_ROOT}/eval_outputs/${EXPERIMENT_NAME}}"
EXPORT_ROOT="${EXPORT_ROOT:-${REPO_ROOT}/checkpoints/exported_hf/${EXPERIMENT_NAME}}"

R0_GPUS="${R0_GPUS:-0,1}"
R1_GPUS="${R1_GPUS:-2,3}"
R2_GPUS="${R2_GPUS:-4,5}"
R3_GPUS="${R3_GPUS:-6,7}"
R0_MASTER_PORT="${R0_MASTER_PORT:-30620}"
R1_MASTER_PORT="${R1_MASTER_PORT:-30611}"
R2_MASTER_PORT="${R2_MASTER_PORT:-30612}"
R3_MASTER_PORT="${R3_MASTER_PORT:-30613}"

TRAIN_STEPS="${TRAIN_STEPS:-1000}"
SFT_WARMUP_STEPS="${SFT_WARMUP_STEPS:-100}"
GROUP_SIZE="${GROUP_SIZE:-8}"
LEARNING_RATE="${LEARNING_RATE:-3e-5}"
WEIGHT_DECAY="${WEIGHT_DECAY:-0.01}"
LR_WARMUP_STEPS="${LR_WARMUP_STEPS:-50}"
KL_BETA="${KL_BETA:-0.02}"
SFT_REPLAY_WEIGHT="${SFT_REPLAY_WEIGHT:-0.2}"
R3_ADAPTIVE_KL_TARGET="${R3_ADAPTIVE_KL_TARGET:-0.03}"
MAX_SEQUENCE_LENGTH="${MAX_SEQUENCE_LENGTH:-18000}"
MAX_BATCH_TOKENS_PER_GPU="${MAX_BATCH_TOKENS_PER_GPU:-524288}"
MAX_BATCH_SIZE_PER_GPU="${MAX_BATCH_SIZE_PER_GPU:-1024}"
EVAL_MAX_BATCH_TOKENS_PER_GPU="${EVAL_MAX_BATCH_TOKENS_PER_GPU:-524288}"
EVAL_MAX_BATCH_SIZE_PER_GPU="${EVAL_MAX_BATCH_SIZE_PER_GPU:-1024}"
SHUFFLE_BUFFER_SIZE="${SHUFFLE_BUFFER_SIZE:-8192}"
VALIDATION_RECORDS="${VALIDATION_RECORDS:-512}"
LOG_INTERVAL="${LOG_INTERVAL:-10}"
EVAL_INTERVAL="${EVAL_INTERVAL:-100}"
SAVE_INTERVAL="${SAVE_INTERVAL:-100}"
BASE_SEED="${BASE_SEED:-20260728}"

R0_BIASES="${R0_BIASES:-0.00 0.10 0.20 0.30 0.50}"
GEN_BATCH_RECORDS="${GEN_BATCH_RECORDS:-1024}"
MAX_WRITE_TOKENS="${MAX_WRITE_TOKENS:-700}"
MAX_MODEL_LEN="${MAX_MODEL_LEN:-32768}"
MAX_NUM_SEQS="${MAX_NUM_SEQS:-1024}"
MAX_NUM_BATCHED_TOKENS="${MAX_NUM_BATCHED_TOKENS:-524288}"
MAX_SEQ_LEN_TO_CAPTURE="${MAX_SEQ_LEN_TO_CAPTURE:-2048}"
GPU_MEMORY_UTILIZATION="${GPU_MEMORY_UTILIZATION:-0.94}"
DECODE_BATCH_SIZE="${DECODE_BATCH_SIZE:-32}"
ASR_BATCH_SIZE="${ASR_BATCH_SIZE:-32}"
AUTOPCP_BATCH_SIZE="${AUTOPCP_BATCH_SIZE:-32}"
AUTOPCP_CHUNK_SIZE="${AUTOPCP_CHUNK_SIZE:-1024}"

TENSORBOARD_PORT="${TENSORBOARD_PORT:-6040}"
TENSORBOARD_SESSION="${TENSORBOARD_SESSION:-simul_stage7a_reward_v2_tensorboard}"
